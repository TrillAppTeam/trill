import 'package:flutter/material.dart';

class RatedAlbumsScreen extends StatelessWidget {
  const RatedAlbumsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Your Albums'),
      ),
    );
  }
}
