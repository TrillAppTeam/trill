import 'package:flutter/material.dart';

class RatedAlbumsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Your Albums'),
      ),
    );
  }
}
