import 'package:trill/models/album.dart';

// will need more fields, this is just for testing
class UserList {
  String name;
  List<Album> albums;

  UserList({required this.name, required this.albums});
}
